import createNode from './createNode';

class linkedList {
  constructor() {
    this.head = null;
    this.size = 0;
  }
  //Insert first node

  //insert last node

  //get at index

  //remove at index
}
